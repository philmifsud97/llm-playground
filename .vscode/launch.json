{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ice breaker runner",
            "type": "python",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${file}",
            "console": "integratedTerminal",
            "envFile": ".env"
        }
    ]
}